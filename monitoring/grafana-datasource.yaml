apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasource
  namespace: deploy-proj-5bf395f0a4fa
  labels:
    app: grafana
data:
  datasource.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        uid: prometheus
        type: prometheus
        access: proxy
        url: http://prometheus.deploy-proj-5bf395f0a4fa:9090
        isDefault: true
